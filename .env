GOOGLE_API_KEY=
MODEL=gemini-2.5-pro
PORT=3002
GS_JSON_PATH="assets/DATASET1.JSON"
GS_CSV_PATH="assets/top100_titles_thumbnails.csv"
GS_DOCX_PATH="assets/Viral_Crime_Niche_Master_Keywords_ALL_CHANNELS_MERGED.docx"

# --- Upload size (10 GB). Use 5 GB if you prefer ---
MULTER_MAX_FILE_SIZE=10737418240

# --- HTTP server timeouts (long uploads & slow networks) ---
SERVER_REQUEST_TIMEOUT_MS=5400000       # 90 minutes total per request
SERVER_HEADERS_TIMEOUT_MS=120000        # 2 minutes to receive headers
SERVER_KEEPALIVE_TIMEOUT_MS=600000      # 10 minutes keep-alive

# --- Gemini Files API activation wait (match request timeout) ---
FILES_ACTIVE_TIMEOUT_MS=5400000         # 90 minutes
FILES_INITIAL_DELAY_MS=1500
FILES_MAX_DELAY_MS=8000

# --- History storage (optional overrides) ---
# Your code uses HIST_DIR and HISTORY_LIMIT_BYTES (NOT DATA_DIR / HISTORY_LIMIT_GB)
HIST_DIR=./data/history
HISTORY_LIMIT_BYTES=21474836480         # 20 GB